import UpdateFlow from '@/components/UpdateFlow';

// Force static generation for the update page
export const dynamic = 'force-static';

export default function UpdatePage() {
  return <UpdateFlow />;
}

